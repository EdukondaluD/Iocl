import React, {} from 'react';
import {
  SafeAreaView,
  StyleSheet
} from 'react-native';

const App = () => {
  return (
    <SafeAreaView style={styles.sectionContainer}>
      
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  sectionContainer: {
    flex:1
  }
});

export default App;
